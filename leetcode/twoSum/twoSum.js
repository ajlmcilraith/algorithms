let twoSumFunction = function(numberArray, target) {
  // Filter function to pick out the two that sum to the target
  //? Rule out any numbers >= to the target? 
  let result = [];
  for (i in numberArray) {
  }
  return result
}

module.exports = twoSumFunction;